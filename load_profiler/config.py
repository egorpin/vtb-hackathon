# Интервал обновления (секунды)
ANALYSIS_INTERVAL = 3

# Настройки подключения к БД
# host = localhost, так как GUI запускается на твоей машине, а не в докере
DB_CONFIG = {
    "dbname": "mydb",
    "user": "user",
    "password": "password",
    "host": "localhost", 
    "port": "5433"
}