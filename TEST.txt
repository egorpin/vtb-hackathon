# Перейдите в папку проекта
cd ~/projects/postgres-load-tester

# Запустите контейнеры
docker-compose up -d

# Подождите 30 секунд для инициализации PostgreSQL
sleep 30

# Запустите полный тест
docker-compose exec metrics-collector /scripts/run_complete_test_suite.sh

# Перейдите в папку проекта
cd ~/projects/postgres-load-tester

# Запустите контейнеры
docker-compose up -d

# Подождите 30 секунд для инициализации PostgreSQL
sleep 30

# Запустите полный тест
docker-compose exec metrics-collector /scripts/run_complete_test_suite.sh

# Перейдите в папку проекта
cd ~/projects/postgres-load-tester

# Запустите контейнеры
docker-compose up -d

# Подождите 30 секунд для инициализации PostgreSQL
sleep 30

# Запустите полный тест
docker-compose exec metrics-collector /scripts/run_complete_test_suite.sh
